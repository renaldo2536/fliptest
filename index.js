/**
 * @format
 */

import {AppRegistry} from 'react-native';
import AppContainer from './page/home';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => AppContainer);
